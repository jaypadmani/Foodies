<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodies">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Foodies">
        <activity
            android:name=".activites.RegistationActivity"
            android:exported="true">

        </activity>




        <activity
            android:name=".activites.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".activites.Welcome_activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".MainActivity"

            android:label="@string/app_name"
            android:theme="@style/Theme.Foodies.NoActionBar">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".activites.DetailedDailyMealActivity">

        </activity>

        <activity
            android:name=".activites.mycart"
            android:exported="true">

        </activity>

    </application>

</manifest>